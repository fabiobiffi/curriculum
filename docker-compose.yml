version: "3.9"

services:
  
  curriculm:
    image: curriculum:latest
    container_name: curriculum
    build: 
      dockerfile: ci/Dockerfile
      context: .
    ports:
      - "8080:8080" 
    volumes:
      - .:/curriculum
    entrypoint: npm run dev -- --port 8080 --host
